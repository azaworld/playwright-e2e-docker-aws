{
  "name": "fur-refer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:fur4": "playwright test tests/fur4-main/fur4-main-site.spec.ts",
    "test:refer": "playwright test tests/fur4-referral",
    "test:refer:fast": "playwright test tests/fur4-referral/pre-login --workers=2 --fully-parallel",
    "test:all": "npx playwright test && npx ts-node tests/utils/upload-playwright-report-to-s3.ts && npx ts-node tests/utils/always-json-reporter.ts",
    "test:critical": "playwright test tests/fur4-main/critical-flows.spec.ts",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "install-browsers": "playwright install",
    "test:daily": "npm run test:all -- --reporter=html,json,junit",
    "monitor:single": "npx ts-node tests/real-time-monitor.ts single",
    "monitor:continuous": "npx ts-node tests/real-time-monitor.ts continuous",
    "monitor:start": "npm run monitor:continuous 5",
    "test:teams": "npx playwright test; npx ts-node tests/utils/upload-playwright-report-to-s3.ts",
    "test:teams:full": "npm run test:teams && npx ts-node tests/utils/upload-playwright-report-to-s3.ts",
    "test:full": "npx playwright test && npx ts-node tests/utils/upload-playwright-report-to-s3.ts && npx ts-node tests/utils/always-json-reporter.ts",
    "docker:build": "docker build -t fur4-playwright-tests .",
    "docker:run": "docker run --rm -e TEAMS_WEBHOOK_URL=$TEAMS_WEBHOOK_URL fur4-playwright-tests",
    "docker:compose:up": "docker-compose up -d",
    "docker:compose:down": "docker-compose down",
    "docker:compose:logs": "docker-compose logs -f",
    "deploy:aws": "chmod +x deploy.sh && ./deploy.sh",
    "test:ci": "npx playwright test --reporter=list,json,html && npx ts-node tests/utils/upload-playwright-report-to-s3.ts && npx ts-node tests/utils/notifyTeams.ts",
    "test:ci:full": "npx playwright test --reporter=list,json,html && npx ts-node tests/utils/upload-playwright-report-to-s3.ts && npx ts-node tests/utils/notifyTeams.ts",
    "test:with-upload": "npx playwright test --reporter=list,json,html && npx ts-node tests/utils/upload-playwright-report-to-s3.ts && npx ts-node tests/utils/notifyTeams.ts",
    "test:complete": "node run-tests-complete.js",
    "test:teams:debug": "npx playwright test --reporter=list,json,html && npx ts-node tests/utils/notifyTeams.ts",

    "test:notify": "npx playwright test && npx ts-node tests/utils/notifyTeams.ts",
    "test:notify:all": "npx playwright test && npx ts-node tests/utils/notifyTeams.ts",
    "open-report": "node open-report.js"
  },
  "keywords": [
    "playwright",
    "testing",
    "automation",
    "fur4"
  ],
  "author": "",
  "license": "ISC",
  "description": "Automated Playwright tests for FUR4.com and refer.fur4.com guest-facing areas",
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^24.0.10",

    "dotenv": "^17.2.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "dayjs": "^1.11.13",
    "node-fetch": "^3.3.2",
    "ssh2-sftp-client": "^12.0.1"
  }
}
